# GoReleaser configuration for XELYON CLI
version: 2

before:
  hooks:
    - go mod tidy
    - go fmt ./...
    - go test ./...

builds:
  - id: xelyon
    binary: xelyon
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64  # Windows ARM64は現状サポート外
    ldflags:
      - -s -w
      - -X github.com/susugadx/xelyon-cli/internal/version.Version={{.Version}}
      - -X github.com/susugadx/xelyon-cli/internal/version.Commit={{.Commit}}
      - -X github.com/susugadx/xelyon-cli/internal/version.Date={{.Date}}

archives:
  - id: xelyon
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - 'README'
  groups:
    - title: Features
      regexp: '^feat:'
      order: 0
    - title: Bug Fixes
      regexp: '^fix:'
      order: 1
    - title: Others
      order: 999

release:
  github:
    owner: susugadx
    name: xelyon-cli
  draft: false
  prerelease: auto
  name_template: "XELYON CLI v{{.Version}}"
  header: |
    ## XELYON CLI v{{.Version}}

    高速・軽量なLLMコーディングアシスタント

    ### インストール方法
    ```bash
    # Linux / macOS installer
    curl -fsSL https://raw.githubusercontent.com/susugadx/xelyon-cli/main/install.sh | bash -s -- --yes

    # Homebrew
    brew install susugadx/tap/xelyon

    # npm wrapper
    npm install -g xelyon

    # Windows PowerShell
    iwr https://raw.githubusercontent.com/susugadx/xelyon-cli/main/install.ps1 -UseB | iex

    # Manual fallback:
    # Download the matching asset below, verify checksums.txt, and place xelyon on PATH.
    ```

    `xelyon` は provider credential 未設定でも TUI を起動します。API key は config に保存せず、環境変数、`xelyon auth openai-subscription login`、または Ollama を使って設定してください。

    ### サポート環境
    - **Linux**: amd64, arm64
    - **macOS**: amd64 (Intel), arm64 (Apple Silicon)
    - **Windows**: amd64

  footer: |
    **Full Changelog**: https://github.com/susugadx/xelyon-cli/compare/{{.PreviousTag}}...{{.Tag}}

    **Documentation**: https://github.com/susugadx/xelyon-cli#readme

# Homebrew Tap configuration
brews:
  - name: xelyon
    repository:
      owner: susugadx
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: https://github.com/susugadx/xelyon-cli
    description: "高速・軽量なLLMコーディングアシスタント"
    license: MIT
    install: |
      bin.install "xelyon"
    test: |
      system "#{bin}/xelyon", "--version"
