# xelyon

NPM wrapper for the XELYON CLI release binary.

```bash
npm install -g xelyon
xelyon
```

The package downloads the matching binary from GitHub Releases during `postinstall` and verifies it with the release `checksums.txt`.
