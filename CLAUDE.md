# XELYON

GoベースのマルチプロバイダーAIコーディングCLI。ソロ開発。

## ビルド・テスト

- `make ci-check` — コミット前必須（go fmt → build → lint → test）
- `make gen-all` — 設定追加時に実行
- `go build ./...`
- `go test ./...`
- `go vet ./...`
- `golangci-lint run`

## アーキテクチャ

```
xelyon-cli/
├── cmd/                  # CLIエントリーポイント
├── internal/
│   ├── agent/            # エージェントロジック（対話ループ）
│   ├── api/              # LLMプロバイダー（DeepSeek/Claude/OpenAI等）
│   ├── audit/            # 監査ログ
│   ├── cache/            # キャッシュ管理
│   ├── config/           # 設定管理
│   ├── crypto/           # 暗号化
│   ├── history/          # セッション管理
│   ├── i18n/             # 国際化
│   ├── lsp/              # LSP連携
│   ├── mcp/              # MCP対応
│   ├── prompt/           # プロンプト管理
│   ├── refactor/         # リファクタリング支援
│   ├── testutil/         # テストユーティリティ
│   ├── tools/            # ツール実行
│   ├── ui/               # スピナー、ページャー
│   └── version/          # バージョン管理
├── docs/                 # ドキュメント
├── examples/             # 使用例
└── scripts/              # ビルド・運用スクリプト
```

## 開発ルール

- コミット前は `make ci-check` 必須
- 新機能・バグ修正時はテストも追加
- エラーハンドリング必須（I/O操作、HTTPにはTimeout設定）
- コミットは日本語で機能単位で小さく、具体的に記述
- 新機能追加時は README.md と docs/ を更新（バグ修正は不要）
- 設定追加時は `make gen-all` を実行
- 未使用コードは作らない（呼び出し元を確認してから実装）
- 公開関数・型には日本語コメント必須（godoc形式）

## コードスタイル

- gofmt準拠
- エラーは必ずハンドルする（`_ = err` 禁止）
- 長いファイル（400行超）は分割読みで見落としが多い → 全体を確認してから編集
- 既存コードのパターンに従う。新パターンを導入する前に確認を取る

## レビュー・調査リクエスト時のルール

ファイル修正や修正提案はしない。発見事項をP0-P3重要度で報告:
- P0: セキュリティ脆弱性、データ損失リスク
- P1: バグ、ロジック誤り
- P2: パフォーマンス、保守性
- P3: スタイル、命名

## やらないこと

- 非コーディングタスクの追加
- インフラ依存の機能（API直接利用の思想を維持）